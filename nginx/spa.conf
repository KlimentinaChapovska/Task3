# Nginx config for serving the React SPA from the frontend container
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React Router — any path that isn't a real file returns index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
